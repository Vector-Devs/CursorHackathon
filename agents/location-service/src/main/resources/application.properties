spring.application.name=LocationService
server.port=${PORT:8095}

# Enterprise service
location-service.enterprise-base-url=http://localhost:8085

# Mock services (vessels, places)
location-service.mock-services-base-url=http://localhost:8082

# Haversine radius (km) for "nearby" cities from vessel positions
location-service.nearby-radius-km=200
